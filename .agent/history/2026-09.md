## 2026-09-20 15:05 — TASK-20260920-1500-MEMORY-BOOTSTRAP — Implantação da memória persistente do agente

### User Request

Implantar arquitetura de memória persistente, checkpoints e recuperação automática do agente antes de iniciar o sistema RAG.

### Objective

Criar `AGENTS.md` e toda a estrutura `.agent/*` + arquivos de instrução auxiliares.

### Result

Todos os 13 artefatos criados e validados (workspace estava vazio, sem migração real necessária).

### Important Decisions

- ADR-001: hierarquia de autoridade com mensagem atual do usuário sempre acima de memória persistida.
- ADR-002: arquitetura planejada do sistema RAG registrada (Qdrant, Postgres, FastAPI, Next.js, adapter LLM dual, auth com aprovação de ADM).

### Validation

Todos os arquivos confirmados no disco; `.vscode/settings.json` validado como JSON.

### Relevant Files

AGENTS.md, .agent/*, .github/copilot-instructions.md, .vscode/settings.json, .stack_tech.md, .stdout-stderr-instructions.md, .answer_instructions.md, versionamento.md

### Final Status

COMPLETED
