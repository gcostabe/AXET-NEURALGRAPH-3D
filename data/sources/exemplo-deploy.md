# Guia de Deploy

Este guia explica como fazer deploy do sistema.

## Pré-requisitos

Docker e Docker Compose instalados.

## Passos

1. Copiar `.env.example` para `.env`.
2. Ajustar variáveis.
3. Executar `docker compose up`.
