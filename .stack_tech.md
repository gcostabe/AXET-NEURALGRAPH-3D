# STACK TECHNICAL REFERENCE

Este arquivo documenta apenas informações confirmadas pela implementação real. Itens planejados mas não implementados ainda estão marcados como **(PLANEJADO)** e não devem ser tratados como verdade de código até serem construídos e validados.

---

## Estado atual

Nenhum código-fonte foi escrito ainda neste workspace. Este arquivo será atualizado conforme cada componente for implementado e validado.

---

## Linguagens e Runtimes (PLANEJADO)

- Backend: Python (FastAPI)
- Frontend: TypeScript/Node.js (Next.js)

## Frameworks (PLANEJADO)

- Backend: FastAPI
- Frontend: Next.js + Tailwind CSS

## Bancos de Dados (PLANEJADO)

- Vector DB: Qdrant
- Relacional: Postgres (usuários, conversas, mensagens, audit_log)

## Embeddings (PLANEJADO)

- Modo local: sentence-transformers (ex: BAAI/bge-m3 ou intfloat/multilingual-e5-large)
- Modo API: endpoint `/v1/embeddings` do API Gateway já instalado pelo usuário
- Seleção configurável via variável de ambiente

## LLM / Inferência (PLANEJADO)

- API Gateway já instalado pelo usuário, compatível com formato OpenAI (`/v1/chat/completions`) e formato Anthropic (`/v1/messages`)
- Camada adapter própria no backend selecionando implementação via `.env` (`LLM_PROVIDER=openai|anthropic`)

## Autenticação (PLANEJADO)

- JWT (access token curto + refresh token em cookie httpOnly)
- Hash de senha: bcrypt ou argon2
- 3 níveis: ADMIN, USER, e status PENDING/BLOCKED controlando acesso

## Orquestração / Infraestrutura (PLANEJADO)

- Docker Compose: serviços `qdrant`, `postgres`, `backend`, `frontend`
- Preparação futura para deploy online: reverse proxy com TLS (Nginx/Caddy), rate limiting

## Estrutura de Diretórios (PLANEJADO)

```text
RAG-LOCAL-REEF/
├── docker-compose.yml
├── .env.example
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── config.py
│   │   ├── auth/
│   │   ├── ingestion/
│   │   ├── retrieval/
│   │   ├── llm/
│   │   └── api/
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   └── Dockerfile
└── data/
    └── sources/
```

## Integrações Externas (PLANEJADO)

- API Gateway do usuário (URL e credenciais via `.env`, ainda não fornecidos)

## Telemetria / Execução

Nenhuma implementada ainda.

## Dependências

Nenhuma instalada ainda (sem `requirements.txt`/`package.json` criados).
